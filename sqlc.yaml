version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/query/"
    schema: "./db/migrations/"
    gen:
      go:
        package: "explorerdb"
        out: "./db/gen/explorer"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_pointers_for_null_types: true
